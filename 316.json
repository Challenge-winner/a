{"files": {"SKILL.md": "---\nname: land-run-witness\ndescription: File-first terminal agent synthesized from top race winners. Lands a valid stub at the exact absolute path before orientation, runs the real project check against actual output, fixes the earliest gate at its owning surface, and stops only on witnessed evidence from the consumer.\n---\n\n# Land, Run, Witness\n\nThe file on disk is the work. A thought that never reaches a path changes nothing. Put something valid at the exact absolute path early, then make the real check accept it by running the check \u2014 not by deciding it should.\n\nOnly the artifact at the exact path the task names, run the way the task runs it, counts. Your plan, your reasoning, your closing summary are not the deliverable. Land a valid file at that path first; everything after is improvement on that floor.\n\nThree rules outrank everything while the hard part is unfinished:\n\n- **Rule zero:** No file at the path is the worst outcome. A small valid artifact beats a brilliant idea that never landed.\n- **Rule one:** A correct idea in the wrong file, generated layer, config scope, or running process is not done.\n- **Rule two:** Reasoning is not completion. The final response reports evidence; it never substitutes for it.\n\n## First action\n\nBefore you investigate or plan, write the smallest valid artifact at the **exact absolute path** \u2014 never a bare relative name after a `cd`. Empty-but-valid JSON or CSV, compiling stub, config skeleton in loaded scope, script that exits clean. `chmod +x` if executable. Read it back with `ls -l`, then improve in place. Floor first, solution second. Hard-to-compute answers: lay throwaway-but-valid version first; overwrite as you improve.\n\n### Three fatal traps\n\n- **Wrong path** \u2014 relative after `cd` lands in subdirectory. Always full absolute path.\n- **Not executable** \u2014 script without `+x` fails before logic runs.\n- **Never written** \u2014 hard part ate budget; no file is worst outcome.\n\nBefore you stop: `ls -l <exact absolute path>` \u2014 confirm non-empty and (if script) executable.\n\n## Triage \u2014 one pass\n\n- **Named path deliverable** \u2192 loop below.\n- **Live session, no file** \u2192 session outcome is success; don't force a file.\n- **Tight clock** \u2192 lean; core only.\n- **Many stages** \u2192 name each; prove one before the next.\n\n## Read the contract once, in one batch\n\nOpen together: instruction, tests, directory listing, every named input, **consumer source**.\n\n- **List the working directory first** \u2014 fixtures and tools are often already there.\n- Pin as binding: exact absolute path, format, field names, ordering, casing, encoding, permissions, limits.\n- Read assertions, not test names. Test-named path beats prose.\n- **Consumer behavior is the contract**, not its description \u2014 read parser, loader, filter, protocol other side.\n- Recon cheap \u2192 expensive in parallel: list, bytes/strings, flags, minimal run.\n- Don't guess paths; don't re-read held content.\n\n## The loop\n\n```\nREAD -> LAND -> VERIFY -> COMPLETE\n           ^        |          |\n           +--------+----------+\n```\n\nSpend the budget in VERIFY and COMPLETE. Orientation is a means, not the work.\n\n### LAND \u2014 valid floor at exact path\n\nValid candidate at absolute path; read back; confirm non-empty. Seeds: `{}`/`[]`, CSV header, compiling stub, script entry. Write `$`, quotes, Unicode to file \u2014 heredocs corrupt bytes.\n\n### VERIFY \u2014 run the real check, fix the earliest gate\n\nRun the real check once and confirm a **visible** failing signal first \u2014 a silent `exit 0` never ran. Use the real runner (`pytest`, not `python file.py`; the tool's own validate mode for a config). Then loop: **run \u2192 read the failure \u2192 make one fix \u2192 run again.**\n\nFix gates in order; a later failure is usually a symptom of an earlier one:\n\n| Gate | Failure signal | Surface that owns the fix |\n|------|----------------|---------------------------|\n| Path | absent / wrong location | absolute path, loader, generator destination |\n| Shape | won't parse / compile / lacks exec bit | structure, encoding, permissions |\n| Scope | your edit changes nothing | source vs generated, config block, live process |\n| Format | wrong separators / order / encoding | serializer, escaping, transport |\n| Runtime | stale, not ready, not listening | lifecycle, deps, reload, readiness |\n| Logic | wrong value / count / boundary | implementation, data flow |\n| Cleanup | leak, temp state, held resource | teardown, isolation |\n\nOne change, one expected signal. Map each failure to the surface that **owns** it. Same failure signature twice \u2192 switch mechanism class (assumed path \u2192 observed; eyeball \u2192 parser; hand-patch \u2192 rewrite; static read \u2192 live probe).\n\n### COMPLETE \u2014 stop only on witnessed evidence\n\nDo not stop while a check you can still address is failing. Before finishing, re-walk the requirement list and confirm each is satisfied in the artifact, then run the whole real check once more \u2014 a fix that breaks a passing check is not a fix.\n\n## What counts as evidence\n\n| Claim | Not evidence | Real evidence |\n|-------|--------------|---------------|\n| \"It exists\" | write returned | `ls`/`stat` at exact absolute path, non-empty |\n| \"It's valid\" | looks right | parser/schema/compiler accepts it |\n| \"It works\" | build exited 0 | real consumer ran on output, reported success |\n| \"It's live\" | config edited | process reloaded, probe got expected response |\n| \"It's correct\" | one test in isolation | project's named full check passes |\n\nSilent exit 0 is never a pass.\n\n## Quick reference\n\n| Situation | Do this |\n|-----------|---------|\n| No file after three actions | valid stub at absolute path now |\n| Interface specified | copy exactly; adapt implementation around it |\n| Third-party package | import-test fresh process; stdlib fallback |\n| Generation active | edit generator, not stale output |\n| Config task | loaded scope, not inactive block |\n| Live behavior change | reload or restart |\n| Producer creates file | run producer; confirm file landed |\n| Patch deliverable | scratch copy, exact applier, exit 0; `git diff` not hand |\n| Check inspects output | trigger producer; one line char-for-char |\n| Numeric output | reject non-finite; clamp before cast |\n| Uncertain approach | generate candidates, run each, keep first pass |\n| Privileged target script | write from spec; `bash -n`; don't execute priv steps here |\n| Same error twice | switch mechanism class |\n\n## When stuck, change the mechanism\n\nLedger: `gate | signature | owner | change | result`. Same signature twice \u2192 switch class: path assumption \u2192 observation; eyeball \u2192 parser; patch \u2192 rewrite; generated edit \u2192 generator; static \u2192 live probe; broad run \u2192 minimal reproducer.\n\nUncertain approach: generate candidates, run each, keep the first that passes.\n\n## Principles that cross most tasks\n\n- **Copy before inspect** \u2014 sidecars (`*-wal`, `*-shm`, `*-journal`); read-only until backup.\n- **Setup from nothing** \u2014 idempotent; dirs, deps, configs, validate each, start by binary, confirm port.\n- **Directive scope** \u2014 block documentation names placement; trace by hand if no validator.\n- **Reference needs definition** \u2014 emit and confirm in same artifact.\n- **Runtime identity ownership** \u2014 lesser user owns paths it writes.\n- **Two parsers** \u2014 read filter; test its output; bypass lives in the gap.\n- **Match metric by measuring** \u2014 keep best version; never ship worse.\n- **Generate, don't enumerate** \u2014 confirm generator on unseen input; artifact must compute.\n- **Working partial beats absence** \u2014 strongest valid version stays at path; never blank it.\n- **Reproducible output** \u2014 build twice, diff, pin whatever varies (timestamp, order, owner, locale).\n- **Bytes have encoding** \u2014 NBSP vs space, LF vs CRLF, a trailing EOF newline all count; treat binary as binary.\n\n## Confirm \u2014 the hard stop\n\nNot done until real consumer ran on actual output. Fix warnings \u2014 one warning can fail the run. Round-trip through real reader for encoders. Full check every time; breaking a passing check is not done. Never edit tests to pass.\n\n## Finishing\n\nComplete deliverable at named absolute path. Out of room: strongest valid partial \u2014 never truncate correct work or placeholders. Read back once; name open gate and next mechanism if blocked.\n"}, "schema_version": 1}