{"files": {"SKILL.md": "---\nname: pipeline-gate-witness-v2\ndescription: Pipeline-first terminal agent synthesized from top race winners. Maps the consumer path before writing, anchors a valid stub at the exact absolute path, moves one gate at a time through the real check, and ships only after the project check accepts actual output.\n---\n\n# Pipeline Gate Witness\n\nThe file on disk is the work. Correctness is what the task's own checks read off the filesystem and run \u2014 not your plan, not your closing message. Trace where your output travels, anchor a valid artifact at the exact absolute path early, then move one gate at a time until the real consumer accepts it.\n\n**Three laws.** (1) No file at the path is the worst possible outcome \u2014 a small valid one beats an unlanded masterpiece. (2) A right answer in the wrong file, wrong scope, or a process you never reloaded is not done. (3) A claim is not a check. Satisfy every requirement you can \u2014 never stop at the first passing signal while others still fail.\n\n## Triage \u2014 one pass\n\n- **Named path deliverable** \u2192 four phases below.\n- **Live session, no file** \u2192 session outcome is success; don't force a file.\n- **Tight clock** \u2192 anchor stub, then core prove only.\n- **Many stages** \u2192 name each; prove one before the next.\n\n## Phase 1 \u2014 Map the pipeline\n\nBefore you write, read the contract in one batch: instruction, tests, directory listing, every named input, **consumer source**.\n\n- List the working directory first \u2014 fixtures and tools are often already there.\n- Pin as binding: exact absolute path, format, field names, ordering, casing, encoding, permissions, limits.\n- Read assertions, not test names. Test-named path beats prose.\n- **Consumer behavior is the contract** \u2014 read parser, loader, filter, protocol other side.\n- Trace the pipeline your output must survive:\n\n```\nauthor \u2192 generation/include \u2192 parse \u2192 normalization \u2192 runtime \u2192 final check\n```\n\nName which stage owns each failure before you edit. Don't guess paths; don't re-read held content.\n\n## Phase 2 \u2014 Prepare and anchor\n\n**Prepare first:** install runners and dependencies; run the real check once; confirm a visible failing signal before deep work.\n\n**Then anchor:** before you investigate further, write the smallest valid artifact at the **exact absolute path** \u2014 never bare relative after `cd`. `{}`/`[]`, CSV header, compiling stub, script exiting 0. `chmod +x` if executable. `ls -l`; confirm non-empty. Improve in place; never delete to start over.\n\nHard-to-compute answers: lay throwaway-but-valid version first; overwrite as you improve. Write `$`, quotes, Unicode to file \u2014 heredocs corrupt bytes.\n\n### Three fatal traps\n\n- **Wrong path** \u2014 relative after `cd` lands in subdirectory. Always full absolute path.\n- **Not executable** \u2014 script without `+x` fails before logic runs.\n- **Never written** \u2014 hard part ate budget; no file is worst outcome.\n\nBefore you stop: `ls -l <exact absolute path>` \u2014 confirm non-empty and (if script) executable.\n\n## Phase 3 \u2014 One gate at a time\n\nLoop until the real consumer accepts output:\n\n```\nRUN -> READ FAILURE -> ONE FIX -> RUN\n ^                              |\n +------------------------------+\n```\n\nFix gates in order:\n\n**Path \u2192 Shape \u2192 Scope \u2192 Format \u2192 Runtime \u2192 Logic \u2192 Cleanup**\n\n| Gate | Signal | Owner surface |\n|------|--------|---------------|\n| Path | absent, wrong location | absolute path, loader, generator destination |\n| Shape | parse/compile/permission fail | structure, encoding, executable form |\n| Scope | edit has no effect | source vs generated, config scope, live process |\n| Format | right intent, wrong form | serializer, escaping, ordering, transport |\n| Runtime | stale, not ready | lifecycle, readiness, environment |\n| Logic | wrong value/count/boundary | implementation, data flow, filter |\n| Cleanup | leak, temp state | teardown, ownership, isolation |\n\nOne change, one expected signal:\n\n- Generation active \u2192 edit **generator**, not stale output\n- Config task \u2192 **loaded** scope, not inactive block\n- Live behavior \u2192 reload/restart; file edit does not move a running process\n- Third-party \u2192 import-test fresh process; stdlib fallback\n- Interface given \u2192 copy exactly; bend implementation around it\n\nSame failure signature twice \u2192 switch mechanism class (path assumption \u2192 observation; eyeball \u2192 parser; patch \u2192 rewrite; static \u2192 live probe).\n\nSilent exit 0 is never a pass.\n\n## Phase 4 \u2014 Witness before stop\n\n- Read artifact back at exact absolute path.\n- Trigger producers when checks inspect output \u2014 one line char-for-char.\n- Round-trip through real reader for encoders.\n- Full project check every time; a fix that breaks a passing check is not done.\n- Never edit tests to pass.\n\n## What counts as evidence\n\n| Claim | Not evidence | Real evidence |\n|-------|--------------|---------------|\n| \"It exists\" | write returned | `ls`/`stat` at exact absolute path, non-empty |\n| \"It's valid\" | looks right | parser/schema/compiler accepts it |\n| \"It works\" | build exited 0 | real consumer ran on output, reported success |\n| \"It's live\" | config edited | process reloaded, probe got expected response |\n| \"It's correct\" | one test in isolation | project's named full check passes |\n\n## Quick reference\n\n| Situation | Do this |\n|-----------|---------|\n| No file after three actions | valid stub at absolute path now |\n| Interface specified | copy exactly; adapt implementation around it |\n| Third-party package | import-test fresh process; stdlib fallback |\n| Generation active | edit generator, not stale output |\n| Config task | loaded scope, not inactive block |\n| Live behavior change | reload or restart |\n| Producer creates file | run producer; confirm file landed |\n| Patch deliverable | scratch copy, exact applier, exit 0; `git diff` not hand |\n| Check inspects output | trigger producer; one line char-for-char |\n| Numeric output | reject non-finite; clamp before cast |\n| Reproducibility | build twice and diff; pin timestamps and ordering |\n| Same error twice | switch mechanism class |\n\n## Principles\n\n- **Copy before inspect** \u2014 sidecars (`*-wal`, `*-shm`, `*-journal`); read-only until backup.\n- **Setup from nothing** \u2014 idempotent; dirs, deps, configs, validate each, start by binary, confirm port.\n- **Directive scope** \u2014 block documentation names placement.\n- **Reference needs definition** \u2014 emit and confirm in same artifact.\n- **Runtime identity ownership** \u2014 lesser user owns paths it writes.\n- **Two parsers** \u2014 read filter; test its output; bypass lives in the gap.\n- **Generate, don't enumerate** \u2014 test generator on unseen input.\n- **Match by measuring** \u2014 keep best version; never ship worse.\n- **Working partial beats absence** \u2014 never blank a valid partial.\n\n## Domain \u2014 when the task matches\n\n**Shell and setup:** `set -euo pipefail`; absolute paths; idempotent `mkdir -p`; start daemon by binary; poll readiness not sleep.\n\n**Archives:** pin `SOURCEDATEEPOCH`, `tar --sort=name`, `--numeric-owner`, `gzip -n`, `LC_ALL=C`; build twice and diff.\n\n**nginx:** directive scope; definitions before references; `nginx -t` then reload; `curl` probe.\n\n**Filters:** read filter source; test its output \u2014 bypass lives in the gap.\n\n**Text bytes:** NBSP vs ASCII space; LF vs CRLF; trailing newline at EOF counts.\n\n**Numerics:** tolerance not `==`; explicit `isnan`; keep best-measuring version.\n\n## Finishing\n\nComplete deliverable at named absolute path. Out of room: strongest valid partial \u2014 never truncate correct work or placeholders. Read back once; name open gate and next mechanism if blocked.\n"}, "schema_version": 1}